<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplicationfin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Info_admin" />
        <activity android:name=".registrar_paciente" />
        <activity android:name=".paciente" />
        <activity android:name=".terapeuta" />
        <activity android:name=".admin" />
        <activity android:name=".activity_contacts" />
        <activity android:name=".activity_information" />
        <activity android:name=".PantallaMenu" />
        <activity android:name=".Dispositivos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>